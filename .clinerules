# 矿山数据处理问题 — 项目规则

## 项目概述

**2025年第二十二届五一数学建模竞赛 B题："矿山数据处理问题"**

项目目标是对矿山监测数据进行数学建模，覆盖五个递进的子问题：

| 问题 | 核心任务 | 关键指标 |
|------|---------|----------|
| P1 | A → B 的线性变换，分析误差来源 | R²、残差分布 |
| P2 | 数据降维压缩 + 还原，分析压缩效率 | 压缩比、MSE ≤ 0.005 |
| P3 | 去噪 + 标准化 + X→Y 建模 + 统计检验 | 拟合优度 |
| P4 | X→Y 建模 + 参数自适应调整算法 | 拟合优度最大化、稳定性 |
| P5 | 高维数据降维 + 重构 + 重构数据与 Y 的关系建模 | 泛化性、复杂度 |

## 运行方式

- **必须使用 `uv`**：`uv run python main.py --problem {1-5}`
- 数据目录：`problemX/附件X/`
- 输出目录：`outputs/problemX/`

## 代码结构约定

```
src/
├── data_loader.py      # 数据加载（读 .xlsx / .npy）
├── linear_model.py     # 线性回归模型
├── metrics.py          # 评价指标（R²、MSE、RMSE、MAE 等）
├── diagnostics.py      # 诊断检验（DW、BP、KS、偏度、峰度等）
├── visualization.py    # 可视化
├── report.py           # Markdown 报告生成
└── problems/
    ├── problem1.py     # P1 实现 ✅（线性变换 + 误差诊断）
    ├── problem2.py     # P2 待实现（降维压缩 + 还原）
    ├── problem3.py     # P3 待实现（去噪 + 标准化 + 建模）
    ├── problem4.py     # P4 待实现（自适应参数调整）
    └── problem5.py     # P5 待实现（降维 + 重构 + 建模）
```

## 开发规范

### 新增 Problem 时
1. 在 `src/problems/problemX.py` 中实现 `solve_problemX(data_dir, output_dir)` 函数
2. 在 `main.py` 的 `solve()` 中添加路由
3. 遵循 P1 的流程模式：`data_loader` → `model` → `metrics` → `diagnostics` → `visualization` → `report`
4. 每个 Problem 完成后生成对应的 `说明.md`（参考 P1 结构）

### 数值规范
- 报告中的数值保留 4 位小数
- 浮点数比较使用 `numpy.isclose()`，不要用 `==`
- 统计检验标注显著性水平（α = 0.05）
- P2 的 MSE 要求不高于 0.005

### 文档规范
- `说明.md` 结构：问题重述 → 数据说明 → 模型假设 → 模型建立与求解 → 结果分析 → 诊断检验 → 结论
- 数学公式使用 LaTeX 语法
- 所有数据路径使用相对路径
- 关键结论用加粗标注

## 当前状态

| Problem | 状态 | 核心结果 |
|---------|------|----------|
| P1 | ✅ 完成 | R²=0.9891，残差近似 U(-3.02, 3.02)，Koenker BP=52.45 |
| P2 | ❌ 待实现 | 降维压缩 + 还原，MSE ≤ 0.005 |
| P3 | ❌ 待实现 | 去噪 + X→Y 建模 + 统计检验 |
| P4 | ❌ 待实现 | 参数自适应调整 + 拟合优度最大化 |
| P5 | ❌ 待实现 | 降维重构 + 重构-Y 关系建模 |

## 禁止事项

- ❌ 不能用系统 Python，必须用 `uv run python`
- ❌ 不能修改 `main.py` 的 CLI 参数接口格式
- ❌ 不能删除 `AGENT.md`、`problem1/report.bk.md`、`weights.npy`
- ❌ 报告数值必须由代码实际运行生成，禁止编造
- ❌ 不能新建与 P1 风格不一致的代码结构

## AGENT.md 维护

- 每完成一个 Problem，更新 AGENT.md 中的进度和关键结果
- 发现新问题或假设时，追加到 AGENT.md 对应章节